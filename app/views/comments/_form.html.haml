= simple_form_for 'comment', url: comments_path(), html: { id: 'clear-form', class: 'comment-form' }, remote: true do |f|
  .form-group
    = hidden_field :comment, :post_id, value: post_id
    = f.input :name, placeholder: '¿Cómo te llamas?', required: true, label: false, error: "Necesito saber cómo te llamas para poder responderte."
  .form-group
    = f.input :email, placeholder: 'Email', required: true, label: false, error: 'El email es obligatorio. No voy a usarlo para fines comerciales.'
  .form-group
    = f.input :content, placeholder: 'Díme que te ha parecido el artículo :)', required: true, label: false, as: :text, error: "Debes escribir algo en el comentario"
  .form-group
    .note
      * Tus datos nos serán añadidos a ninguna lista de suscriptores o similar.
  .form-group
    = recaptcha_tags
  .form-group
    = f.submit "Publicar"
